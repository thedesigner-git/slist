[build]
  builder        = "DOCKERFILE"
  dockerfilePath = "apps/backend/Dockerfile"
  buildContextDir = "apps/backend"

[deploy]
  startCommand  = "uvicorn main:app --host 0.0.0.0 --port $PORT"
  healthcheckPath = "/health"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
