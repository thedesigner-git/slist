services:
  backend:
    build: ./apps/backend
    ports:
      - "8000:8000"
    env_file: ./apps/backend/.env
    volumes:
      - ./apps/backend:/app
    network_mode: host
    restart: unless-stopped
