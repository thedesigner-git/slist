[build]
  base    = "apps/web"
  command = "npm install && npm run build"
  publish = "apps/web/dist"

# SPA fallback — all routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
