---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/ci.yml
  - apps/web/vitest.config.ts
  - apps/web/app/health/route.ts
  - apps/backend/tests/test_health.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []

must_haves:
  truths:
    - "GitHub Actions CI workflow runs lint, typecheck, and test jobs"
    - "Web lint and typecheck pass on a clean checkout"
    - "Backend ruff check and pytest pass"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline for web and backend"
      contains: "ruff check"
    - path: "apps/web/vitest.config.ts"
      provides: "Vitest config for Next.js"
      contains: "vitest"
    - path: "apps/backend/tests/test_health.py"
      provides: "Health endpoint test"
      contains: "test_health"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "apps/web"
      via: "working-directory"
      pattern: "working-directory.*apps/web"
    - from: ".github/workflows/ci.yml"
      to: "apps/backend"
      via: "working-directory"
      pattern: "working-directory.*apps/backend"
---

<objective>
Set up GitHub Actions CI with lint, typecheck, and test jobs for both the web app and the FastAPI backend. Add a minimal test for each to prove the pipeline works.

Purpose: Catch regressions early. Every future plan will be validated by this CI.
Output: .github/workflows/ci.yml, Vitest config, one web test, one backend test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml`:

```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/web/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements-dev.txt
      - run: ruff check .
      - run: pytest
```
  </action>
  <verify>cat .github/workflows/ci.yml | grep "ruff check"</verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains two jobs: "web" and "backend"
    - web job runs: npm run lint, npm run typecheck, npm run test
    - backend job runs: ruff check ., pytest
    - Both jobs use working-directory to scope to their app
  </acceptance_criteria>
  <done>CI workflow defined for both web and backend</done>
</task>

<task type="auto">
  <name>Task 2: Vitest config and web smoke test</name>
  <files>
    apps/web/vitest.config.ts,
    apps/web/app/health/route.ts,
    apps/web/tests/health.test.ts
  </files>
  <read_first>apps/web/package.json</read_first>
  <action>
Create `apps/web/vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config'
import path from 'path'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['**/*.test.ts'],
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, '.') },
  },
})
```

Create `apps/web/app/health/route.ts` — a simple API route for health checks:
```ts
import { NextResponse } from 'next/server'
export function GET() {
  return NextResponse.json({ status: 'ok', service: 'investiq-web' })
}
```

Create `apps/web/tests/health.test.ts` — smoke test (no Supabase dependency):
```ts
import { describe, it, expect } from 'vitest'

describe('health', () => {
  it('returns ok status', () => {
    const result = { status: 'ok', service: 'investiq-web' }
    expect(result.status).toBe('ok')
    expect(result.service).toBe('investiq-web')
  })
})
```

This is intentionally minimal — proves Vitest is configured and runs. Real tests added per feature in future phases.
  </action>
  <verify>cd apps/web && npm run test</verify>
  <acceptance_criteria>
    - apps/web/vitest.config.ts contains "defineConfig" from vitest
    - apps/web/tests/health.test.ts contains "expect(result.status).toBe('ok')"
    - npm run test exits 0 with "1 passed"
    - apps/web/app/health/route.ts returns JSON with status: ok
  </acceptance_criteria>
  <done>Vitest configured and passing with one smoke test</done>
</task>

<task type="auto">
  <name>Task 3: Backend pytest smoke test</name>
  <files>
    apps/backend/tests/__init__.py,
    apps/backend/tests/test_health.py
  </files>
  <read_first>apps/backend/main.py</read_first>
  <action>
Create `apps/backend/tests/__init__.py` (empty).

Create `apps/backend/tests/test_health.py`:
```python
from fastapi.testclient import TestClient
from main import app

client = TestClient(app)

def test_health():
    response = client.get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok", "service": "investiq-api"}
```

Verify ruff passes on the entire backend:
```bash
cd apps/backend && ruff check .
```
  </action>
  <verify>cd apps/backend && pytest tests/test_health.py -v</verify>
  <acceptance_criteria>
    - apps/backend/tests/test_health.py contains "def test_health()"
    - test_health asserts status_code == 200
    - pytest exits 0 with "1 passed"
    - ruff check . exits 0 (no lint errors)
  </acceptance_criteria>
  <done>Backend pytest running with health check test passing</done>
</task>

</tasks>

<verification>
- [ ] cd apps/web && npm run lint — exits 0
- [ ] cd apps/web && npm run typecheck — exits 0
- [ ] cd apps/web && npm run test — 1 passed
- [ ] cd apps/backend && ruff check . — exits 0
- [ ] cd apps/backend && pytest — 1 passed
</verification>

<success_criteria>
- All verification checks pass
- .github/workflows/ci.yml covers both web and backend with correct working directories
- No test infrastructure imports Supabase (keeps tests fast and dependency-free)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
