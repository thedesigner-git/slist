---
phase: 02-data-agent-scheduler
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/agent/edgar.py
autonomous: true
gap_closure: true
requirements:
  - AGENT-01
  - AGENT-05

must_haves:
  truths:
    - "is_us_ticker() correctly identifies BIDU as non-US when its seed market is HK"
    - "EDGAR filings are only fetched for companies whose seed market is US"
    - "Dot-suffix tickers like SAP.DE, 0700.HK, ASML.AS still correctly return False from is_us_ticker"
  artifacts:
    - path: "apps/backend/agent/edgar.py"
      provides: "Market-aware US ticker detection"
      contains: "seed_companies.json"
  key_links:
    - from: "apps/backend/agent/edgar.py"
      to: "apps/backend/data/seed_companies.json"
      via: "JSON load for market lookup"
      pattern: "seed_companies\\.json"
---

<objective>
Fix is_us_ticker() to cross-reference seed_companies.json market field instead of relying solely on dot-suffix heuristic.

Purpose: BIDU (Baidu) is seeded with market='HK' but has no dot suffix in its ticker, so is_us_ticker('BIDU') incorrectly returns True. This causes EDGAR filings to be fetched for a company classified as HK market. The fix should use the seed company market field as the primary check, with dot-suffix as a fallback for tickers not in the seed list.

Output: Updated edgar.py with market-aware ticker classification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-agent-scheduler/02-VERIFICATION.md
</context>

<interfaces>
<!-- Current edgar.py is_us_ticker (line 23-25): -->
```python
def is_us_ticker(ticker: str) -> bool:
    """Only fetch EDGAR data for US tickers (no dot suffix)."""
    return "." not in ticker
```

<!-- runner.py calls get_filings with just the ticker (line 84): -->
```python
filings = get_filings(ticker)
```

<!-- seed_companies.json BIDU entry: -->
```json
{"ticker": "BIDU", "name": "Baidu Inc.", "market": "HK", "exchange": "NASDAQ"}
```

<!-- Seed markets: "US", "DE", "HK", "EU" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update is_us_ticker to cross-reference seed company market data</name>
  <files>apps/backend/agent/edgar.py</files>
  <read_first>
    - apps/backend/agent/edgar.py (full file -- current implementation)
    - apps/backend/data/seed_companies.json (first 10 entries -- understand structure)
    - apps/backend/agent/runner.py (line 84 -- how get_filings is called, only ticker arg)
  </read_first>
  <action>
Modify `apps/backend/agent/edgar.py` to add a cached seed market lookup and update `is_us_ticker()`:

1. Add a new cached function `_load_seed_markets()` that reads `seed_companies.json` (using the same SEED_PATH pattern as runner.py: `Path(__file__).parent.parent / "data" / "seed_companies.json"`) and returns a `dict[str, str]` mapping ticker to market (e.g., `{"AAPL": "US", "BIDU": "HK", "SAP.DE": "DE"}`). Cache with `@lru_cache(maxsize=1)`.

2. Import `json` and `Path` from `pathlib` at the top.

3. Update `is_us_ticker(ticker: str) -> bool`:
   - First check the seed market map: if ticker is in the map, return `market == "US"`.
   - Fallback for tickers not in the seed list: use the existing dot-suffix heuristic (`"." not in ticker`).
   - Update docstring to: `"""Check if ticker is US-market. Uses seed data first, falls back to dot-suffix heuristic."""`

This ensures BIDU (market=HK) returns False, while unknown tickers without dots still return True (conservative default for EDGAR lookup).
  </action>
  <verify>
    <automated>cd apps/backend && python -c "from agent.edgar import is_us_ticker; assert is_us_ticker('AAPL') == True, 'AAPL should be US'; assert is_us_ticker('BIDU') == False, 'BIDU should not be US (market=HK)'; assert is_us_ticker('SAP.DE') == False, 'SAP.DE has dot suffix'; assert is_us_ticker('0700.HK') == False, 'HK ticker has dot suffix'; print('All assertions passed')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/agent/edgar.py` contains `import json`
    - `apps/backend/agent/edgar.py` contains `from pathlib import Path`
    - `apps/backend/agent/edgar.py` contains a function `_load_seed_markets` decorated with `@lru_cache(maxsize=1)`
    - `_load_seed_markets` reads from `seed_companies.json` and returns a dict mapping ticker to market
    - `is_us_ticker("AAPL")` returns True (seed market is US)
    - `is_us_ticker("BIDU")` returns False (seed market is HK)
    - `is_us_ticker("SAP.DE")` returns False (dot suffix, and seed market is DE)
    - `is_us_ticker("UNKNOWN_TICKER")` returns True (not in seed, no dot -- fallback heuristic)
    - `get_filings("BIDU")` returns `[]` (skipped because is_us_ticker returns False)
  </acceptance_criteria>
  <done>is_us_ticker correctly classifies BIDU as non-US by reading its market field from seed_companies.json; dot-suffix fallback preserved for non-seed tickers</done>
</task>

</tasks>

<verification>
- `python -c "from agent.edgar import is_us_ticker; print(is_us_ticker('BIDU'))"` prints `False`
- `python -c "from agent.edgar import is_us_ticker; print(is_us_ticker('AAPL'))"` prints `True`
- `python -c "from agent.edgar import get_filings; print(get_filings('BIDU'))"` prints `[]`
- `grep "seed_companies" apps/backend/agent/edgar.py` returns a match
- `grep "lru_cache" apps/backend/agent/edgar.py` finds at least 2 matches (existing CIK cache + new seed markets cache)
</verification>

<success_criteria>
- BIDU is correctly classified as non-US based on its seed market field
- EDGAR filings are not fetched for BIDU
- All existing dot-suffix tickers (.DE, .HK, .AS) continue to be correctly classified as non-US
- US tickers (AAPL, MSFT, etc.) continue to be correctly classified as US
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-agent-scheduler/02-07-SUMMARY.md`
</output>
